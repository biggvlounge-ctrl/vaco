# VACO -- generated nginx config. Regenerate with
# node deploy/generate-nginx-conf.js --docker

# Real rate limiting -- closes one of the ecosystem audit's flagged
# gaps ("no rate limiting anywhere in the 30 Node apps"). Declared at
# this file's top level rather than inside `server {}` because this
# file is spliced directly into nginx's own `http {}` context (the
# official nginx image's default.conf `include`s everything under
# conf.d/ from inside `http {}`) -- `limit_req_zone` is only legal
# there, not inside `server`/`location`. 10 requests/sec per client
# IP, a real burst of 20 queued before requests start getting 503'd,
# applied once at the server level so it covers every proxied
# location below without repeating it per-app.
limit_req_zone $binary_remote_addr zone=vaco_general:10m rate=10r/s;

server {
    listen 80;
    server_name localhost;

    limit_req zone=vaco_general burst=20 nodelay;
    limit_req_status 503;

    # VDP and VENVS -- each serves its own real `npm run build` output
    # via `serve` inside its own container (Dockerfile.vite); nginx
    # proxies to them rather than reading a shared host `dist/` path.
    location /vdp/ {
        rewrite ^/vdp/(.*)$ /$1 break;
        proxy_pass http://vdp:5174;
        proxy_set_header Host $host;
    }

    location /venvs/ {
        rewrite ^/venvs/(.*)$ /$1 break;
        proxy_pass http://venvs:5173;
        proxy_set_header Host $host;
    }

    # WebSocket support (cvnvo's real-time messaging) -- upgrade
    # headers on every proxied location so ws:// connections work
    # through the reverse proxy.
    location /cvnvo/ {
        rewrite ^/cvnvo/(.*)$ /$1 break;
        proxy_pass http://cvnvo:8798;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /v4-proxy/ {
        rewrite ^/v4-proxy/(.*)$ /$1 break;
        proxy_pass http://v4-proxy:8787;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /v4-search/ {
        rewrite ^/v4-search/(.*)$ /$1 break;
        proxy_pass http://v4-search:8788;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vacon/ {
        rewrite ^/vacon/(.*)$ /$1 break;
        proxy_pass http://vacon:8805;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vacon-c/ {
        rewrite ^/vacon-c/(.*)$ /$1 break;
        proxy_pass http://vacon-c:8809;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /hvntz/ {
        rewrite ^/hvntz/(.*)$ /$1 break;
        proxy_pass http://hvntz:8792;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /void/ {
        rewrite ^/void/(.*)$ /$1 break;
        proxy_pass http://void:8793;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /voidmagic/ {
        rewrite ^/voidmagic/(.*)$ /$1 break;
        proxy_pass http://voidmagic:8797;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /voken/ {
        rewrite ^/voken/(.*)$ /$1 break;
        proxy_pass http://voken:8794;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vago/ {
        rewrite ^/vago/(.*)$ /$1 break;
        proxy_pass http://vago:8795;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vxllage/ {
        rewrite ^/vxllage/(.*)$ /$1 break;
        proxy_pass http://vxllage:8796;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /yap/ {
        rewrite ^/yap/(.*)$ /$1 break;
        proxy_pass http://yap:8802;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /chopz/ {
        rewrite ^/chopz/(.*)$ /$1 break;
        proxy_pass http://chopz:8800;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /chopz-shop/ {
        rewrite ^/chopz-shop/(.*)$ /$1 break;
        proxy_pass http://chopz-shop:8801;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vacay/ {
        rewrite ^/vacay/(.*)$ /$1 break;
        proxy_pass http://vacay:8803;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vavlt-stvdios/ {
        rewrite ^/vavlt-stvdios/(.*)$ /$1 break;
        proxy_pass http://vavlt-stvdios:8808;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vsafe/ {
        rewrite ^/vsafe/(.*)$ /$1 break;
        proxy_pass http://vsafe:8799;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vaca/ {
        rewrite ^/vaca/(.*)$ /$1 break;
        proxy_pass http://vaca:8804;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /v3/ {
        rewrite ^/v3/(.*)$ /$1 break;
        proxy_pass http://v3:8811;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /shield/ {
        rewrite ^/shield/(.*)$ /$1 break;
        proxy_pass http://shield:8812;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vaco-analytics/ {
        rewrite ^/vaco-analytics/(.*)$ /$1 break;
        proxy_pass http://vaco-analytics:8790;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vulture-music/ {
        rewrite ^/vulture-music/(.*)$ /$1 break;
        proxy_pass http://vulture-music:8806;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vulture-flix/ {
        rewrite ^/vulture-flix/(.*)$ /$1 break;
        proxy_pass http://vulture-flix:8807;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vulture-pods/ {
        rewrite ^/vulture-pods/(.*)$ /$1 break;
        proxy_pass http://vulture-pods:8810;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /venvm/ {
        rewrite ^/venvm/(.*)$ /$1 break;
        proxy_pass http://venvm:8813;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /dreams/ {
        rewrite ^/dreams/(.*)$ /$1 break;
        proxy_pass http://dreams:8814;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vulture-studios/ {
        rewrite ^/vulture-studios/(.*)$ /$1 break;
        proxy_pass http://vulture-studios:8815;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vex/ {
        rewrite ^/vex/(.*)$ /$1 break;
        proxy_pass http://vex:8816;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vex-trading/ {
        rewrite ^/vex-trading/(.*)$ /$1 break;
        proxy_pass http://vex-trading:8817;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vaco-notify/ {
        rewrite ^/vaco-notify/(.*)$ /$1 break;
        proxy_pass http://vaco-notify:8818;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vaco-audit/ {
        rewrite ^/vaco-audit/(.*)$ /$1 break;
        proxy_pass http://vaco-audit:8819;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vaco-operator/ {
        rewrite ^/vaco-operator/(.*)$ /$1 break;
        proxy_pass http://vaco-operator:8820;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vaco-media/ {
        rewrite ^/vaco-media/(.*)$ /$1 break;
        proxy_pass http://vaco-media:8821;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # The front door: vaco-shell, the real app launcher + session host.
    location / {
        proxy_pass http://vaco-shell:8789;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
