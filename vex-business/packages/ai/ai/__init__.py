"""Vex Business — ai package.

Scaffolded in Phase 1 (repository/Docker/configuration) so the real
workspace structure the Master Directive's Section 5 specifies exists
and installs cleanly. No business logic lives here yet — that's
explicitly Phase 3+ scope per the user's own phased build instruction,
not attempted in Phase 1-2. Left empty on purpose rather than filled
with a placeholder implementation that would be worse than absent
(e.g. a risk module that always approves, or a signal module that
returns a fabricated score) — see README.md's own "Phase status"
section.
"""
