{
  "name": "vaca",
  "version": "0.1.0",
  "private": true,
  "description": "VACA -- the ecosystem's identity/authenticity verification layer, one of V3's three real, distinct components (VCoin, VASH, VACA). A real, standalone, app-agnostic attestation service other apps call into over HTTP.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.13.0"
  },
  "engines": {
    "node": ">=22"
  }
}
