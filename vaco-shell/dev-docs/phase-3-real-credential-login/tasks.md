# Tasks — Phase 3: real credential login UI

- [x] Add `POST /api/register` / `POST /api/login` proxy routes to
      `server.js`, matching the existing `/api/session` proxy shape.
- [x] Add a real password field + Register/Log-in buttons to
      `public/index.html`, alongside the existing quick-login box.
- [x] Real Playwright pass: register a new account, log out, log back
      in with the right password, wrong password shows a real error,
      original quick-login still works.
- [x] Updated `README.md`.

## Next
Nothing further planned for this specific piece.
