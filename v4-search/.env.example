# Copy this file to .env if you need to override the default port.
# The default adapter (MemorySearchAdapter) needs no credentials — it's
# an in-process index seeded in adapters/memoryAdapter.js. A real vendor
# (Elasticsearch, Algolia, etc.) would add its own keys here once chosen.
PORT=8788
