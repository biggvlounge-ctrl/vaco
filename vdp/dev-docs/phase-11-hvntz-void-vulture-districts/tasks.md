# Tasks — Phase 11: HVNTZ, VOID, Vvltvre Music/Pods/Flix districts

- [x] Grepped each of the 5 apps' own real routes/lib functions --
      no field or flow guessed.
- [x] Wrote `hvntzClient.js` / `voidClient.js` /
      `vultureMusicClient.js` / `vulturePodsClient.js` /
      `vultureFlixClient.js` (thin clients, no business logic).
- [x] Wrote `HvntzView.jsx` / `VoidView.jsx` / `VultureMusicView.jsx` /
      `VulturePodsView.jsx` / `VultureFlixView.jsx`.
- [x] Added 5 new `DISTRICTS` entries to `world.js`: filled the 4th
      row's 2 remaining slots (`hvntz`, `void`), added a real 5th row
      for the 3 Vvltvre divisions; grew `WORLD_HEIGHT` 1140 -> 1420.
- [x] Updated `world.js`'s own contentType-vocabulary header comment.
- [x] Wired all 5 into `WorldView.jsx`: imports, `DISTRICT_COLORS`,
      the contentType/id render switch, header comment.
- [x] `vite build` -- clean compile, 69 modules, no import errors.
- [x] Live Playwright pass against all 8 real running servers: walked
      to and completed a real, full flow in every one of the 5 new
      districts in one continuous session -- real bounty, real dual
      payout, real streaming-revenue payout, real cross-app episode
      publish, real gated watch + stream session.
- [x] Updated `README.md` and this dev-docs pair.

## Next
HVNTZ/VOID/the Vvltvre apps each have far more real depth than what's
demoed here (VOID alone has 18+ verticals, HVNTZ has neighbor-match
suggestions and ad review, Vvltvre Music has label deals and manager
rosters) -- each district demos one real, complete loop, not
everything the app can do. Not a gap in the district itself, a
deliberate scope choice matching every prior `-embed` district's own
posture.
