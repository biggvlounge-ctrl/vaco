# Tasks — Phase 18: The Venus Resort Complex

- [x] Duplication check on all four incoming VDP documents; file three
      new, append status to the one already present
- [x] Verify each document's claims against the code; record the four
      that do not hold
- [x] `venusResort.js` — two venues, docks, movement, clamped camera
- [x] VOID water taxi: dock-only boarding, timed crossing, arrival read
      from the clock rather than fired by a timer
- [x] Seating guarded on the player's *current* venue
- [x] Boarding stands the player up, rather than trusting the caller to
- [x] NPC roles (dealer/host/security); table open derived from dealer
- [x] `assertNoRealMoney` — throws, per the outstanding compliance flag
- [x] `VenusResortView.jsx`, rendering `VagoView` unchanged at a table
- [x] Promote the `vago` district: `vago-embed` -> `vdp-native`
- [x] `vdp/test/venusResort.test.js` — VDP's first suite, 25 tests
- [x] Mutation-test all eight rules; all eight caught
- [x] Fix both Vite frontends' broken `shieldAuth` import (pre-existing)
- [x] `scripts/test/frontend-imports.test.mjs` so it cannot silently
      recur; mutation-tested
- [x] `vite build` green for both frontends
