{
  "name": "void",
  "version": "0.1.0",
  "private": true,
  "description": "VOID -- real-world execution platform: the genuinely software-buildable slice (station network, drone routing, service marketplace, load management, scheduling, deterministic dispatch rules), not the AI agents or physical hardware.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=22"
  }
}
