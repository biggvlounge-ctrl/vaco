PORT=8797
# VOID MAGIC never builds its own financial ledger, per the brief's
# own explicit Section 16 rule -- every payment/settlement call routes
# through V3. venvs-mock-backend stands in for the real V3 ledger in
# this environment, same pattern as every other VACO backend.
V3_API_URL=http://localhost:8811
