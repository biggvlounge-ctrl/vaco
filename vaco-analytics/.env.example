# Copy this file to .env if you need to override the default port.
# The default store (metricsStore.js) is in-process and needs no
# credentials. A real ClickHouse/VeloDB deployment (per
# VACO_ANALYTICS_MANAGEMENT_DASHBOARD.md's recommendation) would add
# its own connection details here once one is actually provisioned.
PORT=8790
