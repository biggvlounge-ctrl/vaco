# Tasks — Phase 6: Village Branding

- [x] Clarify two real ambiguities before touching anything: whether
      "Note" is a real data-model rename or UI-copy branding (answer:
      branding only, "Post is fine"), and whether "Village" now
      describes the whole app or stays scoped to the existing
      sub-feature (answer: the whole app; VXLLAGE stays the technical
      name).
- [x] `README.md` — new title ("Village (VXLLAGE)"), the real brand
      statement/tagline, the six-comparable list mapped to what's
      actually built vs. not, a real naming note on the Village/
      Villages overlap.
- [x] `README.md` — `lib/posts.js`'s own "What's here" entry gets a
      real, scoped naming note ("Note" for future UI copy only, no
      code renamed).
- [x] `README.md` — the Call/Live "Not yet built" bullet updated to
      name both real Call surfaces (Zoom, Zoom Business) instead of
      one generic "Zoom-style," with the real, honest gap that neither
      is specified nor built.
- [x] Write this plan/tasks pair.

## Next
None from this pass specifically. If Call ever gets built, the real
distinction between "Zoom" and "Zoom Business" needs a real spec
first — not invented here.
