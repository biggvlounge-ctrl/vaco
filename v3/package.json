{
  "name": "v3",
  "version": "0.1.0",
  "private": true,
  "description": "V3 -- the ecosystem's canonical VCoin/VASH ledger, one of V3's own three real, distinct components (VCoin, VASH, VACA). Split out of venvs-mock-backend's own real, already-tested, drop-in-compatible contract into its own standalone, app-agnostic service.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=22"
  }
}
