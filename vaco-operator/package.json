{
  "name": "vaco-operator",
  "version": "1.0.0",
  "description": "VACO OPERATOR — the one place operator authority is issued and verified",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
