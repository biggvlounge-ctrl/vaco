PORT=8794
# Real payouts (card sales, auction settlements) route through V3's
# /api/vcoin/transfer -- venvs-mock-backend stands in for the real V3
# ledger in this environment (see that project's own README for the
# same caveat).
V3_API_URL=http://localhost:8811
