{
  "name": "vacon",
  "version": "0.1.0",
  "private": true,
  "description": "VACON -- the ecosystem's real operating network: the agent registry MIA and every named executive (QVAN, Leslie, Deskins, Kevin, Kay, Gibson, DREA, Anderson, Stephanie, Ava, Autumn, Jacobi, Jake) actually run inside, with V4 as its interface layer.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=22"
  }
}
