PORT=8798
# Phase 1 (profiles + Gale-Shapley matching) moves no VCoin. Kept here
# for when Gift Dating (VCoin threshold gate) lands, matching every
# other VACO backend's pattern.
V3_API_URL=http://localhost:8811
