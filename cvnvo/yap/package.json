{
  "name": "yap",
  "version": "0.1.0",
  "private": true,
  "description": "Yap -- CVNVO's decoupled dating-safety report app, modeled on the real Tea app. A separate, standalone app from CVNVO itself, per explicit instruction, calling back into CVNVO's real profile API for reporter verification rather than owning a local copy of profile data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=22"
  }
}
